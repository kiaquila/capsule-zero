name: AI Review

on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
      - ready_for_review
  workflow_dispatch:
    inputs:
      ref:
        description: Git ref to review
        required: true
        type: string
      pr_number:
        description: Pull request number
        required: true
        type: string

permissions:
  contents: read
  pull-requests: write
  issues: write

concurrency:
  group: ai-review-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  ai-review:
    if: github.event_name == 'workflow_dispatch' || github.event.pull_request.draft == false
    name: AI Review
    runs-on:
      - self-hosted
      - macOS
      - ai-runner

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ inputs.ref || github.ref }}

      - name: Run local AI review
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_NUMBER: ${{ inputs.pr_number }}
          AI_REVIEW_AGENT: ${{ vars.AI_REVIEW_AGENT }}
          CODEX_CLI_PATH: ${{ vars.CODEX_CLI_PATH }}
          CLAUDE_CLI_PATH: ${{ vars.CLAUDE_CLI_PATH }}
          CLAUDE_REVIEW_MODEL: ${{ vars.CLAUDE_REVIEW_MODEL }}
        shell: bash
        run: /opt/homebrew/bin/pwsh -NoProfile -NonInteractive -ExecutionPolicy Bypass -File ./scripts/run-ai-pr-review.ps1

      - name: Print review diagnostics
        if: always()
        shell: bash
        run: |
          /opt/homebrew/bin/pwsh -NoProfile -NonInteractive -Command '
            $tempRoot = if ($env:RUNNER_TEMP) { $env:RUNNER_TEMP } else { $env:TEMP }
            $publicDiagnosticPath = Join-Path $tempRoot "ai-review-public-diagnostics.log"
            $publicRawExcerptPath = Join-Path $tempRoot "ai-review-public-raw-excerpt.log"

            if (Test-Path $publicDiagnosticPath) {
              Write-Host "--- ai-review-public-diagnostics.log ---"
              Get-Content $publicDiagnosticPath
            }

            if (Test-Path $publicRawExcerptPath) {
              Write-Host "--- ai-review-public-raw-excerpt.log ---"
              Get-Content $publicRawExcerptPath
            }
          '
