# Implementation Plan: Production Docker Compose Supabase Runtime

**Branch**: `codex/docker-compose-prod-stage` | **Date**: 2026-06-26 | **Spec**: `.specify/specs/023-docker-compose-deploy/spec.md`

## Summary

Promote the Docker Compose PR from web-only packaging to a production-shaped runtime: the web app runs against self-hosted Supabase core services, real persisted storage/database/auth, and explicit external-provider gates instead of mock provider responses.

## Technical Context

**Language/Version**: TypeScript, React 19.2, Next.js 16.2.6 App Router, Node 22 Docker base image
**Primary Dependencies**: Docker Compose, Supabase self-hosted services, `@supabase/supabase-js`, `@supabase/ssr`, Postgres 17, pgvector
**Storage**: Supabase Postgres and Supabase Storage named Docker volumes
**Testing**: Repo checks, API contract check, lint, typecheck, Next build, Docker build, Compose config, Compose smoke, Supabase Auth/PostgREST/Storage smoke, browser smoke
**Target Platform**: Linux VM with Docker Engine and Docker Compose
**Project Type**: Next.js web app plus self-hosted backend services
**Performance Goals**: Use production Next standalone image and Supabase services with health/dependency ordering
**Constraints**: No production secrets in git, EN/RU MVP scope unchanged, external providers must not silently mock production behavior
**Scale/Scope**: Compose topology, provider adapter, migrations, env templates, operation docs

## Constitution Check

- Glassmorphism/Achromatic UI: N/A; no user-facing UI styling changes.
- Capsule methodology: PASS; methodology stays behind the existing provider port and database-backed catalog data.
- Direct, Not Dictate: PASS; external provider gates explain what is configured or pending instead of pretending credentials exist.
- Premium quality bar: PASS; startup health, migration idempotency, and browser smoke reduce deployment ambiguity.
- Three upload methods: PASS for provider boundaries; storage and marketplace import now use real Supabase/external-gated paths rather than mock URLs.
- Engineering reuse: PASS; implementation reuses the existing provider contracts, health route, migrations, storage policies, and route/page call sites.

## Verification

| Acceptance criterion | Evidence |
| --- | --- |
| SC-001 feature memory guard | Passed on 2026-06-26 inside `npm run preflight`: `Feature-memory gate passed via .specify/specs/023-docker-compose-deploy/{spec,plan,tasks}.md`. |
| SC-002 local repo/app checks | Passed on 2026-06-26 before PR publication and again after AI Review fixes: `npm run preflight` completed feature-memory, repo baseline, API contract, lint, typecheck, Next build, and tests. `docker build --target runner -t capsule-zero-web:ci ./app` also completed successfully and exported `capsule-zero-web:ci`. |
| SC-003 Compose config renders | Passed on 2026-06-26: `CAPSULE_HOST_PORT=3100 SITE_URL=http://127.0.0.1:3100 ADDITIONAL_REDIRECT_URLS=http://127.0.0.1:3100/en/auth,http://localhost:3100/en/auth docker compose --env-file deploy/compose.env.example config >/tmp/capsule-zero-compose-config.yml`; output had 636 rendered lines. |
| SC-004 real stack starts | Passed on 2026-06-26: `CAPSULE_HOST_PORT=3100 SITE_URL=http://127.0.0.1:3100 ADDITIONAL_REDIRECT_URLS=http://127.0.0.1:3100/en/auth,http://localhost:3100/en/auth docker compose --env-file deploy/compose.env.example up -d --build web`; image `capsule-zero-web:local` rebuilt and web container recreated. |
| SC-005 services healthy and migrate exits 0 | Passed on 2026-06-26: `docker compose --env-file deploy/compose.env.example ps -a` showed `auth`, `db`, `functions`, `imgproxy`, `kong`, `meta`, `rest`, `storage`, `studio`, `supavisor`, `web`, and `realtime` healthy; `capsule-zero-migrate-1` showed `Exited (0)`. |
| SC-006 health reflects real Supabase mode | Passed on 2026-06-26: `curl -fsS http://127.0.0.1:3100/api/health | jq .` returned `providerMode: "supabase"`, `supabase: "configured"`, `storage: "configured"`, `semanticSearch: "configured"`, and `pending-gate` for missing external provider credentials. |
| SC-007 Supabase Auth creates profile | Passed on 2026-06-26: POST to `http://127.0.0.1:8000/auth/v1/signup` with anon key created a real auth user; service-role REST query to `/rest/v1/profiles?select=*` showed the matching `public.profiles` row; cleanup command deleted the temporary `compose-smoke-%@example.test` user. |
| SC-008 browser smoke | Passed on 2026-06-26 after rebuild: in-app browser loaded `http://127.0.0.1:3100/en`, title `Capsule Zero`, H1 `When new money mindset meets old money taste`, and no browser console errors. |
| SC-009 GitHub merge-readiness | Pending after commit/push: mark PR #45 ready for review, update PR body, trigger `@codex review`, then wait for `baseline-checks`, `guard`, `AI Review`, and `osv-scan`. |
| SC-010 AI Review follow-up fixes | Source evidence: `app/src/features/auth/session.ts` now signs the app session cookie and only accepts the legacy mock cookie in non-production mock mode; `app/src/lib/providers/supabase/index.ts` verifies persisted Supabase access tokens, records marketplace confirmations with `wardrobe_entries.item_id`, and calls `spend_coins_atomic`; `supabase/migrations/0004_atomic_coin_spend.sql` adds the atomic debit RPC. Runtime evidence: `CAPSULE_HOST_PORT=3100 SITE_URL=http://127.0.0.1:3100 ADDITIONAL_REDIRECT_URLS=http://127.0.0.1:3100/en/auth,http://localhost:3100/en/auth docker compose --env-file deploy/compose.env.example up migrate` applied `0004_atomic_coin_spend.sql`, emitted `NOTIFY`, and exited 0. RPC smoke evidence: a temporary Supabase Auth user with 6 coins called `/rest/v1/rpc/spend_coins_atomic` twice with the same idempotency key; final `profiles.coin_balance` was `1`, ledger rows for the key were `1`, and both RPC responses returned `profile_coin_balance=1`. |
| SC-011 AI Review second follow-up fixes | Source evidence: `app/src/lib/providers/supabase/index.ts` now upserts completed upload assets by `(bucket, object_path)` when attaching them to created items, resolves processed image URLs from stored `processedBucket`/`processedPath` during polling, and calls `credit_coins_atomic` for paid Lava webhook replays; `supabase/migrations/0005_atomic_coin_credit.sql` adds the atomic purchase credit RPC. Runtime evidence: `CAPSULE_HOST_PORT=3100 SITE_URL=http://127.0.0.1:3100 ADDITIONAL_REDIRECT_URLS=http://127.0.0.1:3100/en/auth,http://localhost:3100/en/auth docker compose --env-file deploy/compose.env.example up migrate` applied `0005_atomic_coin_credit.sql`, emitted `NOTIFY`, and exited 0. RPC smoke evidence: a temporary Supabase Auth user called `/rest/v1/rpc/credit_coins_atomic` twice with the same idempotency key and 15 coins; final `profiles.coin_balance` was `15`, ledger rows for the key were `1`, and both RPC responses returned `profile_coin_balance=15`. |
| SC-012 AI Review third follow-up fixes | Source evidence: `app/src/features/auth/session.ts` routes `readMockSession` through provider auth verification in Supabase mode; `app/src/lib/providers/supabase/index.ts` preserves existing profile display name/locale during auth sync and upserts upload-completion assets by `(bucket, object_path)`. Local verification passed on 2026-06-26: `npm run preflight`, `git diff --check`, and `docker build --target runner -t capsule-zero-web:ci ./app` all exited 0. |
| SC-013 AI Review fourth follow-up fixes | Source evidence: `app/src/features/auth/session.ts` persists Supabase refresh tokens and exposes verified `readAppSession`; `app/src/lib/providers/supabase/index.ts` refreshes expired Supabase sessions before `getUser()` verification and maps catalog search filters to SQL/local filters; `app/src/features/auth/actions.ts` returns provider auth failures through `{ ok: false, message }`; `supabase/migrations/0006_catalog_search_filters.sql` applies category/color/wardrobe filters in `search_catalog_hybrid`. Runtime evidence: `docker compose --env-file deploy/compose.env.example up migrate` applied `0006_catalog_search_filters.sql` and exited 0; a psql smoke query returned `no_match_count=0`, `category_mismatches=0`, and `color_mismatches=0`. Local verification passed on 2026-06-26: `npm run preflight`, `git diff --check`, and `docker build --target runner -t capsule-zero-web:ci ./app` all exited 0. |
| SC-014 AI Review fifth follow-up fixes | Source evidence: `docker-compose.yml` marks `deploy/runtime.env` optional for the web service; `app/src/lib/providers/supabase/index.ts` keeps session reads side-effect free during rendering, ignores non-storage marketplace image URLs when attaching assets, and normalizes color IDs/HEX values before local catalog post-filtering. Local verification passed on 2026-06-27: `docker compose --env-file deploy/compose.env.example config` rendered 634 lines, `npm run preflight`, `git diff --check`, and `docker build --target runner -t capsule-zero-web:ci ./app` all exited 0. |
| SC-015 AI Review sixth follow-up fixes | Source evidence: `supabase/migrations/0004_atomic_coin_spend.sql` and `0005_atomic_coin_credit.sql` revoke atomic billing RPC execution from `anon` and `authenticated`; `app/src/lib/providers/supabase/index.ts` does not write cookies during session reads and returns redacted fixture counts from public health. Local verification passed on 2026-06-27: `npm run preflight`, `git diff --check`, and focused SQL grant inspection for `public`, `anon`, `authenticated`, and `service_role` all exited 0. |
| SC-016 AI Review seventh follow-up fixes | Source evidence: `docker-compose.yml` now leaves external-provider secrets to optional `deploy/runtime.env` instead of overriding them with empty `environment` values; `app/src/lib/providers/supabase/index.ts` refreshes expired or near-expired Supabase sessions in memory before trusted `getUser()` verification without calling cookie mutation helpers. Local verification passed on 2026-06-27: `npm run preflight`, `docker compose --env-file deploy/compose.env.example config`, `git diff --check`, focused `web.environment` inspection, and `docker build --target runner -t capsule-zero-web:ci ./app` all exited 0; rendered `web.environment` contains only app/Supabase connection keys and no external provider secret keys. |
| SC-017 Codex eighth follow-up fixes | Source evidence: `app/src/lib/providers/supabase/index.ts` rejects private storage object paths not prefixed by the current user id before service-role asset upserts/signing, and branches Lava invoice lookup/status updates so non-UUID provider invoice ids only filter `lava_invoice_id`. Local verification passed on 2026-06-27: `npm run preflight`, `git diff --check`, focused grep for `assertUserOwnedStoragePath`/`isUuid` call sites, `docker compose --env-file deploy/compose.env.example config`, and `docker build --target runner -t capsule-zero-web:ci ./app` all exited 0. |
| SC-018 Codex ninth follow-up fixes | Source evidence: `app/src/features/auth/session.ts` can persist refreshed Supabase sessions when cookies are writable; `app/src/lib/providers/{contracts,mock,supabase}/` return upload target `storagePath`; `app/src/lib/providers/supabase/index.ts` preserves marketplace HTTP image URLs through explicit external assets and normalizes requested category aliases before local catalog post-filtering. Local verification passed on 2026-06-27: `npm run preflight`, `docker compose --env-file deploy/compose.env.example config`, focused grep for `canWriteAppSessionCookie`/`EXTERNAL_ASSET_BUCKET`/`buildRequestedCategorySet`/`storagePath`, `git diff --check`, and `docker build --target runner -t capsule-zero-web:ci ./app` all exited 0. Fresh Codex review and GitHub gate evidence will be tracked on PR #45 after push so the reviewed SHA stays stable. |
| SC-019 Codex tenth follow-up fixes | Source evidence: `app/src/features/auth/session.ts` exposes `canWriteAppSessionCookie()` as a zero-max-age probe for writable request boundaries, and `app/src/lib/providers/supabase/index.ts` calls it before Supabase `setSession()` so read-only Server Component renders validate the current access token without consuming the refresh token. Local verification passed on 2026-06-27: `npm run preflight`, `docker compose --env-file deploy/compose.env.example config`, focused grep for `canWriteAppSessionCookie`/`setSession`/`COOKIE_WRITE_PROBE`, `git diff --check`, and `docker build --target runner -t capsule-zero-web:ci ./app` all exited 0. Fresh Codex review and GitHub gate evidence will be tracked on PR #45 after push so the reviewed SHA stays stable. |
| SC-020 Codex eleventh follow-up fixes | Source evidence: `app/src/proxy.ts` refreshes expired/near-expired Supabase app sessions before Server Component validation and propagates the refreshed signed app cookie through middleware request overrides; `docker-compose.yml` sets PostgREST JWT verification to `${JWT_JWKS:-${JWT_SECRET}}` for asymmetric-key mode with legacy fallback. Local verification passed on 2026-06-27: `npm run preflight`, `docker compose --env-file deploy/compose.env.example config`, `JWT_JWKS='{\"keys\":[{\"kty\":\"RSA\",\"kid\":\"demo\"}]}' docker compose --env-file deploy/compose.env.example config`, focused grep for `maybeRefreshAppSessionCookie`/`applyRequestHeaderOverrides`/`PGRST_JWT_SECRET`, `git diff --check`, and `docker build --target runner -t capsule-zero-web:ci ./app` all exited 0. Fresh Codex review and GitHub gate evidence will be tracked on PR #45 after push so the reviewed SHA stays stable. |
| SC-021 Codex twelfth follow-up fixes | Source evidence: `app/src/lib/providers/supabase/index.ts` enforces one 5 second abort budget across the background-removal source image fetch and Photoroom provider call, persists upload-job `timeout` on abort, and marks created marketplace imports `failed` when the provider fetch throws before a response is received. Local verification passed on 2026-06-27: `npm run preflight`, `docker compose --env-file deploy/compose.env.example config`, focused grep for `BACKGROUND_REMOVAL_TIMEOUT_MS`/`createTimeoutController`/`updateMarketplaceImportStatus`, `git diff --check`, and `docker build --target runner -t capsule-zero-web:ci ./app` all exited 0. Fresh Codex review and GitHub gate evidence will be tracked on PR #45 after push so the reviewed SHA stays stable. |
| SC-022 Codex thirteenth follow-up fixes | Source evidence: `app/src/proxy.ts` wraps proxy-level Supabase session refresh, signed-session parsing, response payload parsing, and refreshed cookie signing in a best-effort guard that returns `null` on failure so normal routing and downstream session validation/redirect behavior handle the request. Local verification passed on 2026-06-27: `npm run preflight`, `docker compose --env-file deploy/compose.env.example config`, focused grep for `maybeRefreshAppSessionCookie`/`try`/`catch`, `git diff --check`, and `docker build --target runner -t capsule-zero-web:ci ./app` all exited 0. Fresh Codex review and GitHub gate evidence will be tracked on PR #45 after push so the reviewed SHA stays stable. |
| SC-023 Codex fourteenth follow-up fixes | Source evidence: `app/src/lib/providers/supabase/index.ts` routes capsule creation through `create_capsule_atomic`, maps public catalog search result `userId` to neutral `catalog`, and `supabase/migrations/0007_create_capsule_atomic.sql` validates requested wardrobe entry ownership before inserting `capsule_items` while committing or rolling back the capsule row, palette colors, category targets, and item memberships together. Local verification passed on 2026-06-27: `npm run preflight`, `docker compose --env-file deploy/compose.env.example config`, focused grep for `create_capsule_atomic`/`CAPSULE_ITEM_NOT_OWNED`/`userId: \"catalog\"`, `git diff --check`, `CAPSULE_HOST_PORT=3100 SITE_URL=http://127.0.0.1:3100 ADDITIONAL_REDIRECT_URLS=http://127.0.0.1:3100/en/auth,http://localhost:3100/en/auth docker compose --env-file deploy/compose.env.example up migrate` applied `0007_create_capsule_atomic.sql` and exited 0, and `docker build --target runner -t capsule-zero-web:ci ./app` exited 0. Fresh Codex review and GitHub gate evidence will be tracked on PR #45 after push so the reviewed SHA stays stable. |
| SC-024 Codex fifteenth follow-up fixes | Source evidence: `app/src/lib/providers/supabase/index.ts` wraps marketplace provider `response.json()` parsing and `candidates` shape validation in the same persisted failure path used for provider fetch and non-OK HTTP responses, updating the created import to `failed` before returning. Local verification passed on 2026-06-27: `npm run preflight`, `docker compose --env-file deploy/compose.env.example config`, focused grep for `response.json()`/`MARKETPLACE_IMPORT_CANDIDATES_INVALID`/`SC-024`, `git diff --check`, and `docker build --target runner -t capsule-zero-web:ci ./app` all exited 0. Fresh Codex review and GitHub gate evidence will be tracked on PR #45 after push so the reviewed SHA stays stable. |
| SC-025 Codex sixteenth follow-up fixes | Source evidence: `docs_capsule_zero/project/devops/docker-compose-deploy.md` now documents an ordered deploy sequence that starts dependencies with `--wait`, recreates the canonical `migrate` service with `docker compose up --force-recreate --no-deps migrate`, and only then starts `web`, preventing migration-only releases from reusing an old completed `migrate` container. Local verification passed on 2026-06-27: `npm run preflight`, `docker compose --env-file deploy/compose.env.example config`, focused grep for `--force-recreate --no-deps migrate`/`SC-025`/`FR-050`, `git diff --check`, and `docker build --target runner -t capsule-zero-web:ci ./app` all exited 0. Fresh Codex review and GitHub gate evidence will be tracked on PR #45 after push so the reviewed SHA stays stable. |
| SC-026 Codex seventeenth follow-up fixes | Source evidence: `deploy/supabase/db/apply-capsule-migrations.sh` creates `capsule_zero_internal.schema_migrations`, revokes public/anon/authenticated access, copies rows from the old `public.capsule_zero_schema_migrations` table when present, drops the old public table, and reads/writes future versions only through the private ledger. Local verification passed on 2026-06-27: `npm run preflight`, `docker compose --env-file deploy/compose.env.example config`, `sh -n deploy/supabase/db/apply-capsule-migrations.sh`, focused grep for `capsule_zero_internal.schema_migrations`/`SC-026`/`FR-051`, `git diff --check`, `docker compose --env-file deploy/compose.env.example up --force-recreate --no-deps migrate` exited 0 and skipped already-applied `0001`-`0007` from the private ledger, a psql privilege query returned no public tracking table, private ledger count `7`, and `anon`/`authenticated` privileges `f`, and `docker build --target runner -t capsule-zero-web:ci ./app` exited 0. Fresh Codex review and GitHub gate evidence will be tracked on PR #45 after push so the reviewed SHA stays stable. |
| SC-027 Codex eighteenth follow-up fixes | Source evidence: `ColorPoint` now preserves optional catalog IDs, Supabase color resolution prefers `color.id` before HEX fallback, hydrated catalog colors include `row.id`, Journey catalog colors preserve palette IDs, and unset non-production provider mode stays on mock while Compose explicitly sets `CAPSULE_PROVIDER_MODE=supabase` and production rejects mock. Local verification passed on 2026-06-27: `npm run preflight`, `docker compose --env-file deploy/compose.env.example config`, focused grep for `catalog.has(color.id)`/`id: row.id`/`isNonProductionMockProviderMode`/`FR-052`/`FR-053`, `git diff --check`, and `docker build --target runner -t capsule-zero-web:ci ./app` all exited 0. Fresh Codex review and GitHub gate evidence will be tracked on PR #45 after push so the reviewed SHA stays stable. |
| SC-028 Critic/architect/OMX follow-up fixes | Source evidence: `app/src/features/auth/actions.ts` and `AuthPanel.tsx` now surface confirmation-required Supabase signup as a successful inline auth state; `app/src/features/auth/session-codec.ts`, `session.ts`, and `proxy.ts` share the app-session codec and require `SESSION_SIGNING_SECRET`; `app/src/lib/providers/supabase/index.ts` guards user-scoped service-role operations with a verified session and requires real Lava product IDs; `docker-compose.yml` keeps Edge Functions JWT verification on by default, removes service-role secrets from functions, and uses a JSON-aware web healthcheck; `scripts/deploy-compose.mjs` backs `npm run deploy:compose`. Local verification passed on 2026-06-27: `git diff --check`, `npm --prefix app run lint`, `npm --prefix app run typecheck`, `npm --prefix app run build`, `docker compose --env-file deploy/compose.env.example config`, `node scripts/check-runtime-env.mjs --env app/.env.local.example --allow-placeholders`, `node scripts/check-runtime-env.mjs --env deploy/stage.env.example --surface web,billing,image --allow-placeholders`, `node scripts/check-runtime-env.mjs --env deploy/prod.env.example --surface web,billing,image --allow-placeholders`, `node --check scripts/deploy-compose.mjs`, `npm run check:repo`, `npm run check:api-contract`, and `npm run check:feature-memory -- --worktree` all exited 0. Fresh Codex review and GitHub gate evidence will be tracked on PR #45 after push so the reviewed SHA stays stable. |
| SC-029 Codex nineteenth follow-up fixes | Source evidence: `app/src/lib/providers/supabase/index.ts` now scopes marketplace external asset object paths by `userId/itemId/encodedUrl`, while `readExternalAssetUrl()` decodes the final path segment so existing `userId/encodedUrl` references remain readable. Local verification passed on 2026-06-27: `npm --prefix app run lint`, `npm --prefix app run typecheck`, `npm --prefix app run build`, `docker compose --env-file deploy/compose.env.example config`, and `git diff --check` all exited 0. Fresh Codex review and GitHub gate evidence will be tracked on PR #45 after push so the reviewed SHA stays stable. |
| SC-030 Codex twentieth follow-up fixes | Source evidence: `app/src/lib/providers/supabase/index.ts` now loads the Lava invoice before replay status handling and skips non-paid status updates when the stored invoice is already `paid`, preventing out-of-order `failed`/`expired` events from downgrading a credited invoice. Local verification passed on 2026-06-27: `npm --prefix app run lint`, `npm --prefix app run typecheck`, `npm --prefix app run build`, `docker compose --env-file deploy/compose.env.example config`, `npm run check:feature-memory -- --worktree`, and `git diff --check` all exited 0. Fresh Codex review and GitHub gate evidence will be tracked on PR #45 after push so the reviewed SHA stays stable. |
| Negative scenario 1 no real production secrets in git | Source evidence: stage/prod env examples use `replace-with-*` placeholders; `deploy/compose.env.example` labels demo JWT values local-only and instructs rotation before shared/stage/prod use. |
| Negative scenario 2 app change has feature memory | Source evidence: this PR updates `.specify/specs/023-docker-compose-deploy/spec.md`, `plan.md`, and `tasks.md`. |
| Negative scenario 3 blank external providers stay pending-gate | Source evidence: `app/src/lib/providers/supabase/index.ts` now reports core Supabase/storage readiness separately from external SaaS gates, and Lava health requires API URL/key plus all three real Lava product IDs before moving from `pending-gate` to `configured`; `docker-compose.yml` healthcheck now requires `/api/health` JSON `ok: true` so the container status follows core readiness instead of HTTP 200 alone. |
| Negative scenario 4 migrations are idempotent | Runtime evidence: after web rebuild, `migrate` logs showed `Skipping already applied Capsule Zero migration: 0001_initial_schema.sql`, `0002_storage_policies.sql`, and `0003_runtime_provider_alignment.sql`, then exited 0. |
| Stage/prod env templates validate | Passed on 2026-06-26: `npm run check:runtime-env -- --env deploy/stage.env.example --allow-placeholders` and `npm run check:runtime-env -- --env deploy/prod.env.example --allow-placeholders`. |
| Whitespace safety | Passed on 2026-06-26: `git diff --check` exited 0. |

## SENAR Done Gate

- [x] Feature memory names goal and scope in `spec.md`.
- [x] Verification table names evidence for every acceptance criterion.
- [x] Negative scenarios are covered.
- [x] `tasks.md` process memory is updated before completion.
- [ ] PR description includes the SENAR Done Gate checklist.
- [ ] GitHub required checks are green on the final head SHA.
- [ ] A fresh Codex review is triggered on the final head SHA.

## Project Structure

```text
app/src/lib/providers/supabase/index.ts
deploy/supabase/
docker-compose.yml
supabase/migrations/0003_runtime_provider_alignment.sql
docs_capsule_zero/project/devops/docker-compose-deploy.md
```

## Complexity Tracking

The implementation intentionally adds a substantial self-hosted service topology. The complexity is contained in `docker-compose.yml`, `deploy/supabase/`, and one Supabase provider adapter while preserving existing UI and provider contracts.
