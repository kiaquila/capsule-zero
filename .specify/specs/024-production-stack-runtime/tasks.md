# Tasks 024 — Production Stack Runtime

## Tasks

> Track granular work here as it lands. Each task should resolve to a concrete commit or PR. Phase headers match `plan.md`.

### Phase 1 — nginx + web (current PR)

- [x] Write `infra/nginx/nginx.conf` (main config: events, http, gzip, log format, websocket upgrade map)
- [x] Write `infra/nginx/conf.d/capsulezero.conf` (server :80 with ACME + redirect; server :443 with TLS + ACME + reverse_proxy to `web:3000`)
- [x] Replace top-level `docker-compose.yml` scaffold with `nginx + web` minimal stack
- [x] Remove `docker-compose.dev.yml` (no Phase 1 need; comes back with Postgres/Kratos in Phase 2)
- [x] Slim `deploy/compose.env.example` to Phase 1 contract + documented legacy `/app` Supabase placeholders
- [x] Add `docs_capsule_zero/project/devops/nginx-reverse-proxy.md` (bootstrap, renewal, migration, rollback)
- [x] Update ADR-001 (API-gateway row Traefik → nginx; new "Why nginx and not Traefik or Caddy" section)
- [x] Update Phase 4 council DI-017 (Traefik → nginx revision)
- [x] Update CLAUDE.md, AGENTS.md, constitution.md tech-stack rows
- [x] Update spec.md, plan.md, this tasks.md for the phased delivery model
- [ ] First droplet rollout: stop Caddy, `certbot certonly --standalone`, `docker compose up -d`, smoke `curl https://capsulezero.app/en` (operator-driven; evidence lands on PR after the rollout)

### Phase 2 — Auth vertical slice (working registration/login)

One slice to a **working** end-to-end auth flow on the existing `/app` UI (which is already provider-abstracted). Delivered and verified end-to-end (acceptance 9a–9d, 11–13).

- [x] Add `postgres` (plain `postgres:16`, pgvector deferred by ADR-007) with healthcheck + persistent `pgdata` volume, loopback-bound; PgBouncer deferred by ADR-007
- [x] Add `infra/postgres/00-kratos-db.sh` (provision `kratos` role + database + app DB in one first-init pass)
- [x] Add Postgres/Kratos-DB env keys to `deploy/compose.env.example`; refresh `infra/README.md` to the real nginx + postgres layout
- [x] Add `kratos` service (`oryd/kratos`) + one-shot `kratos-migrate`, attaching to the prepared `kratos` DB
- [x] Add `infra/kratos/` identity schema (`traits.email`, `traits.name.first`, `traits.locale`) + self-service flows; Resend SMTP courier (prod placeholder), MailHog in dev
- [x] Reintroduce `docker-compose.dev.yml` with the MailHog override for the Kratos courier
- [x] Scaffold the Go `api` auth/profile context: `GET /api/health`, Kratos session validation, `profiles` table + `kratos_identity_id → profiles.id` mapping, profile/session endpoints; embedded SQL migrations at boot
- [x] nginx routes `/api/*` to the Go API and Kratos public `/self-service/*` + `/sessions/*` paths in both edge paths: Docker nginx → `api:8080` / `kratos:4433`; active host nginx → `127.0.0.1:8080` / `127.0.0.1:4433` (`auth_request` snippet lands with the first protected server-rendered route in a later slice — the `api` provider talks to the Go API server-side)
- [x] Add the `api` provider mode in `app/src/lib/providers/api/` (AuthPort + ProfileRepository) against the Go API; register it, default `CAPSULE_PROVIDER_MODE=api`; unmigrated domains inherit mock fixtures rebound to the real session user
- [x] Verify acceptance 9a–9d + 11–13: registration + login work end-to-end on `/app` (Playwright e2e green)

### Phase 3 — Redis + remaining `/api/*` surface
- [ ] Add `redis` service with persistent volume; Redis queue consumer goroutines inside `/api` (standalone `/worker` deferred by ADR-007)
- [ ] Widen the Go API + `api` provider to wardrobe / capsule / catalog / billing, retiring the matching Supabase provider paths per domain

### Phase 4 — Storage + email + imgproxy

- [x] Provision Hetzner Object Storage buckets: asset buckets in project
  `15203114` / HEL; Object-Locked backup bucket in project `15296835` / FSN
- [x] Add spec-040 private S3 adapter, storage health dependency, authenticated original-photo init/complete, compose env allowlist, and synchronized OpenAPI client
- [x] Create bucketless runtime-key project `15302873` and backup-key project
  `15302925`; apply cross-project private/public/backup action policies and pass
  the redacted data-plane, control-plane, cross-bucket, and backup read/list/
  delete negative matrix
- [x] Verify exact browser-facing CORS permits `https://capsulezero.app`,
  attacker-origin CORS is denied, backup CORS is absent, and atomically rotate
  the protected server env (`root:root` mode `600`, uploads false)
- [x] Revoke the superseded same-project runtime/backup credentials and both
  temporary policy operators
- [x] Complete the final redacted signed 10 MiB PUT/read/cleanup smoke with the
  rotated dedicated runtime credential (spec 040)
- [x] Resend domain verified for `no-reply@capsulezero.app`; SPF + DKIM published
- [ ] Add `imgproxy` service

### Phase 5 — Observability + backups

- [ ] Configure syslog rotation and OTLP trace exporter target; Grafana remains deferred by ADR-007
- [ ] Nightly age-encrypted `pg_dump` cron uploading to the backup object-storage bucket with 14-day retention; reject Object Lock headers on `PutObject` and explicitly accept/close the remaining provider residual before activation

### Phase 6 — Supabase provider retirement (no `/app` rename)
- [ ] Remove `app/src/lib/providers/supabase/` and `@supabase/*`; drop `supabase` from `ProviderMode`
- [ ] Delete the unused `/web` placeholder directory
- [ ] Delete `docker-compose.legacy-supabase.yml`
- [ ] Drop legacy Supabase env keys from `deploy/compose.env.example` and droplet `.env`

## Process Memory

> Write Dead Ends, Decisions, and Known Issues **before** declaring each phase complete. Future agents inherit this on read.

### Dead Ends

- 2026-06-27 PR #48 explored a 10-service compose scaffold (Traefik + Kratos + Postgres + …) shipping in a single iteration. Rejected after the migration plan turned out to need a separate, smaller "swap the reverse proxy" step ahead of any data service. The scaffold becomes the long-term target; this spec now ships it phase by phase.
- 2026-06-28 considered `--standalone` certbot inside a sidecar container so the compose stack would be self-contained for TLS. Rejected: it duplicates what the `certbot` apt package already does on the host with a `certbot.timer`, and forces an extra container running 24/7 to do work that fires twice a day.
- 2026-06-28 considered exporting the existing Caddy ACME state to nginx-readable PEM files. Rejected: Caddy stores certs in a JSON envelope that has to be parsed and rewritten; cleaner to run a fresh `certbot certonly --standalone` during the migration window.
- 2026-06-30 considered creating the Kratos database/role later, alongside the Kratos container. Rejected: Postgres `/docker-entrypoint-initdb.d` scripts run **only** on first init of an empty volume. Provisioning `kratos` in a later commit would skip a populated droplet volume, leaving Kratos pointed at a non-existent database. We provision both databases up front in one pass.
- 2026-06-30 considered a `.sql` init file for the Kratos role. Rejected: plain SQL files in initdb cannot read env vars, so the role password could not come from the droplet `.env`. Used a `.sh` script (`00-kratos-db.sh`) that reads `KRATOS_DB_*` and guards `CREATE DATABASE` via `\gexec`.
- 2026-06-30 (Go API) first submitted Kratos self-service flows to the `ui.action` URL from the flow response. Rejected: that URL is rendered against Kratos's `SERVE_PUBLIC_BASE_URL` (browser-facing, `127.0.0.1:4433`), so from inside the `api` container it resolves to the container's own loopback → `connection refused`. Fixed by building the submit URL from the API's own internal Kratos base (`http://kratos:4433`) + the flow `id`.
- 2026-06-30 (Go API) planned to use `golang-migrate` (per the original backend-docs). Used a minimal embedded idempotent migrator instead (schema_migrations tracking table, per-file transaction): fewer dependencies, and there is no local Go toolchain to run `go mod tidy` against a heavier dep tree — builds happen in a `golang` container. backend-docs "Migrations" line updated to match.
- 2026-06-30 (e2e) the registration spec flaked once: two tests ran on parallel workers and `Date.now()` collided → duplicate email; argon2 `iterations: 2` also pushed sign-up near the 15 s `waitForURL`. Fixed with unique emails (`Date.now()`+random), `argon2 iterations: 1` (Kratos default), and 25 s waits. Green at 3.7 s afterward.

### Decisions

- 2026-06-27 PR #48 review fix: service-level `./.env` references in `docker-compose.yml` use `env_file` object form with `required: false` so `docker compose ... config` works on a fresh checkout before secrets are present.
- 2026-06-27 PR #48 review fix: the insecure Traefik dev dashboard published by `docker-compose.dev.yml` binds to `127.0.0.1:8081`, matching the file comment and avoiding exposure on shared hosts. (Superseded 2026-06-28 — `docker-compose.dev.yml` is dropped until Phase 2 reintroduces MailHog.)
- 2026-06-27 PR #48 review fix: `npm run deploy:compose` now explicitly targets `docker-compose.legacy-supabase.yml`; the production-stack deploy command lands with spec 024 implementation once real Dockerfiles/configs exist.
- 2026-06-27 PR #48 review fix: the production scaffold uses `pgvector/pgvector:pg16` instead of vanilla `postgres:16-alpine` so `CREATE EXTENSION vector` can succeed when migrations land. (**Superseded 2026-06-30** — v0.1 ships plain `postgres:16`; pgvector is deferred to the semantic catalog-search slice per ADR-007, since users/profiles/wardrobe are relational and nothing queries vectors yet.)
- 2026-06-27 PR #48 review fix: Traefik ACME uses Cloudflare DNS-01 via `CF_DNS_API_TOKEN`. (Superseded 2026-06-28 — see nginx decision below.)
- 2026-06-27 PR #48 review fix: API and worker fallback DSNs derive both username and database from `POSTGRES_USER` / `POSTGRES_DB`, matching the compose env template instead of hard-coding `capsule_zero`. (Carries into Phase 3.)
- 2026-06-27 PR #48 review fix: `deploy/compose.env.example` now describes the production-stack env contract instead of the legacy Supabase runtime. Phase 1 slims this further to the `nginx + web` keys plus the legacy `/app` placeholders the bundle still imports at boot.
- 2026-06-27 PR #48 review fix: the OpenAPI security scheme originally modeled the Kratos browser session as a browser cookie, and generated API clients landed in the canonical `/web` and React Native TypeScript paths while mirroring legacy compatibility outputs until the old scaffolds are removed. (**Superseded 2026-07-01** — the Phase 2 Go API consumes a bearer session token or `X-Session-Token`, not the raw Kratos browser cookie.)
- 2026-06-27 PR #48 review fix: the legacy `npm run deploy:compose` helper was retired instead of keeping a second Supabase env contract beside the production-stack `deploy/compose.env.example`.
- 2026-06-27 PR #48 review fix: `check-feature-memory.mjs` now treats `api/`, `worker/`, `web/`, and `mobile/` as product roots alongside legacy `app/`, and the active workflow docs describe the same gate.
- 2026-06-27 PR #48 review fix: Traefik routed browser-visible Kratos self-service paths through a priority-100 router. (Superseded 2026-06-28 — see nginx decision below; nginx will do this via `location ^~ /self-service/` in Phase 2.)
- **2026-06-28 nginx replaces Traefik as the v0.1 reverse proxy.** Rationale: universally understood directives, smallest mental tax for ops engineers joining later, no `docker.sock` mount required on the edge container, first-class `auth_request` for Kratos, first-class `limit_req_zone` for rate-limit. ADR-001 § "Why nginx and not Traefik or Caddy" carries the full reasoning. The Phase 1 PR replaces the previous 10-service compose scaffold with a minimum-shape `nginx + web` stack and keeps `docker-compose.legacy-supabase.yml` in the repo until Phase 6.
- **2026-06-28 Caddy on host is retired during Phase 1 droplet rollout.** Migration sequence in `nginx-reverse-proxy.md`: stop and disable `caddy.service`; `certbot certonly --standalone -d capsulezero.app` (port 80 free because Caddy is down and the new compose is not up yet); `docker compose up -d`. After this, certbot runs as the host renewal manager via `certbot.timer` and the deploy hook reloads nginx in-place. (Updated 2026-07-01: the edge is host-managed nginx now — the compose `nginx` service is a `docker-edge` rollback profile, so plain `docker compose up -d` starts only the application services.)
- **2026-06-28 incremental phased delivery.** Originally spec 024 shipped as a single big PR. The droplet currently has no production traffic, but rolling 10 services at once still risks a multi-day rollback if any one of them has a config bug. Phased delivery makes each PR independently verifiable and revertable.
- **2026-06-28 keep `/app`, defer rename to `/web` to Phase 6.** Phase 1 builds `web` from `app/Dockerfile` to avoid a rename diff that would obscure the actual compose change being reviewed.
- **2026-06-28 retain legacy Supabase env keys in `compose.env.example` as documented placeholders.** Superseded 2026-07-01 for the production `api` path: the keys remain in the sample env for rollback / explicit legacy mode, but `web` no longer imports the shared env file as runtime environment. Keys are removed in Phase 6 alongside the Supabase provider removal.
- **2026-06-28 PR #49 review fix: certbot webroot is host-managed, not a Docker named volume.** Host `certbot.timer` writes HTTP-01 challenge files to `/var/www/certbot`; nginx bind-mounts that path read-only via `CERTBOT_WEBROOT_HOST_DIR` and serves it from both the port-80 and port-443 ACME locations so HTTP-01 renewals still work after an HTTP→HTTPS redirect. The nginx container healthcheck uses `/nginx-health`, a real static endpoint, instead of probing a challenge token that certbot creates only during renewals.
- **2026-06-28 PR #49 review fix: nginx resolves the `web` service dynamically.** Static nginx upstream blocks resolve Docker service names only at config load, so replacing the `web` container can leave nginx proxying to a removed container IP. The Phase 1 config now uses Docker embedded DNS (`127.0.0.11`) plus variable `proxy_pass` to re-resolve `web:3000` with a short TTL.
- **2026-06-29 PR #50 review fix: spec 024 aligns with ADR-007's slim v0.1 runtime.** Phase 2 uses direct Postgres URLs without PgBouncer, Phase 3 runs the Redis queue consumer inside `api` instead of deploying a standalone `worker`, and Phase 5 verifies syslog/OTLP/backups without requiring Grafana. Promotion triggers for all three deferred services stay in ADR-007.
- **2026-06-30 (founder direction) Phase 2 is reshaped into one working auth vertical slice, not a data/identity/API split.** An earlier same-day plan split Phase 2 into "2a data tier / 2b identity tier" and ran the smoke against the Kratos API only, deferring the UI. Rejected by the founder as shipping stubs: the `/app` frontend is **already built** on the provider port/adapter abstraction (`app/src/lib/providers/`, ports in `contracts.ts`; modes `mock` + `supabase`). The slice instead delivers Postgres + Kratos + the Go API auth/profile context + a new `api` provider in `/app`, so **registration/login actually work end-to-end on the existing UI**. The UI is reused, not rebuilt — the auth form (email/password/name, `requiresEmailConfirmation`) maps onto Kratos self-service + verification.
- **2026-06-30 add a third provider mode `api`; retire the Supabase provider domain by domain.** The supabase provider (~2979 lines) is the entire backend integration, not just auth. New mode `api` implements the ports via typed fetch to the Go API + Kratos; each domain slice removes its supabase counterpart. `mock` stays for local dev/tests. `/app` is **not** renamed to `/web` — that was a cosmetic aspiration; the empty `/web` placeholder is dropped when supabase is fully gone.
- **2026-06-30 Postgres ships plain `postgres:16`; pgvector deferred to the semantic-search slice (ADR-007).** v0.1 entities are relational; nothing queries vectors until catalog semantic search (US-012).
- **2026-06-30 Phase 2 keeps the app role as the image superuser (`POSTGRES_USER`).** A dedicated least-privilege app role is deferred as a hardening follow-up; v0.1 uses the entrypoint-managed role for the app DB and a separate `kratos` login role for the Kratos DB.
- **2026-06-30 the `api` provider composes real auth/profiles + mock fixtures for unmigrated domains.** `createApiProviderRegistry()` spreads a mock registry and overrides `auth`, `profiles`, and `health` with real Go-API-backed ports. Keeps the app fully navigable (dashboard/wardrobe render fixtures) while auth is real; each later slice replaces one mock port with its real `api` port. `mock` mode stays for local/tests.
- **2026-06-30 the Go API drives Kratos "API"-type self-service flows server-side.** The `api` provider (Next.js `server-only`) calls the Go API, which calls Kratos — one backend for the frontend. Registration uses a `session` hook so sign-up auto-logs-in (matches the existing UI, which redirects to the dashboard on success). Email/browser self-service links still reach Kratos public endpoints through nginx; the admin API remains private.
- **2026-06-30 Kratos originally ran with `--dev` during the auth-slice buildout.** Superseded 2026-07-01: once public self-service routes were wired through nginx, base production compose dropped `--dev`; the local dev override keeps it for mkcert/loopback development only.
- **2026-07-01 PR #57 review fix: public `/api/*` is routed at both nginx edges.** The Docker nginx config already routes `/api/` to `api:8080`, but the active production path is host/systemd nginx with compose publishing the Go API on `127.0.0.1:8080`. Added the matching host route and verified the host config pair with `nginx:1.27 nginx -t` using a temporary test cert.
- **2026-07-01 PR #57 review fix: Postgres init quotes env-sourced role, database, and password values before SQL execution.** `00-kratos-db.sh` now passes values as psql variables and uses identifier/literal quoting plus `format('%I'/'%L')` before `\gexec`. Verified with a fresh `postgres:16` container using `KRATOS_DB_USER=kratos.user`, `KRATOS_DB_NAME=kratos-db`, and a password containing a single quote and spaces.
- **2026-07-01 PR #57 review fix: `api` mode rebinds inherited mock wardrobe/capsule fixtures to the real profile UUID.** Real auth/profile now return the Go API profile id, so fallback mock fixtures owned by `MOCK_USER` would otherwise make authenticated dashboards look empty until the wardrobe slice lands. The `api` provider keeps create operations on the real user id and only rebinds inherited fixture reads/updates for not-yet-migrated domains.
- **2026-07-01 PR #57 review fix: profile locales are constrained to EN/RU and profile storage errors stay visible as 5xx.** The auth schema now checks `locale IN ('en', 'ru')`; profile writes normalize/validate the same active locale set; only `profiles.ErrNotFound` returns 404, unsupported locale returns 400, and all other profile repository failures return 500.
- **2026-07-01 PR #57 review fix: profile display-name edits win over stale Kratos traits.** `EnsureForIdentity` still refreshes email and seeds a display name for empty profiles, but it no longer rewrites a user-edited `profiles.display_name` from the original Kratos registration trait on every session/profile resolution.
- **2026-07-01 PR #57 review fix: recovery keeps account enumeration private without hiding infrastructure failures.** Kratos recovery `200` and validation-style `400` remain safe success for the user-facing flow, while unexpected Kratos/courier statuses now return an error; the auth handler surfaces those as `502 INTERNAL_ERROR`. (**Dormant from 2026-07-01** — the recovery flow is disabled and its UI affordance removed this slice; this behavior stays in the retained plumbing and re-activates with the recovery/verification slice.)
- **2026-07-01 PR #57 review fix: the web `api` provider surfaces recovery failures instead of reporting false success.** `requestPasswordRecovery` now mirrors sign-in/sign-up status handling and throws `RECOVERY_FAILED` when the Go API returns a 4xx/5xx recovery error. (**Dormant from 2026-07-01** — retained but no longer reachable from the UI this slice; see the recovery/verification deferral decision below.)
- **2026-07-01 PR #57 review fix: Kratos compose healthcheck uses Kratos's own CLI instead of shell/wget.** `oryd/kratos:v1.1.0` exposes `kratos remote status`, so the service healthcheck now uses exec form against the admin endpoint (`127.0.0.1:4434`) and no longer depends on `/bin/sh` or `wget` existing in the distroless image.
- **2026-07-01 PR #57 review fix: production Kratos secrets are required indexed env keys.** Base compose now requires `SECRETS_COOKIE_0` and `SECRETS_CIPHER_0` instead of falling back to committed dev constants; the dev override owns local-only defaults. This matches Kratos's list-valued env override semantics and fails fast when the droplet env omits a real secret.
- **2026-07-01 PR #57 review fix: Kratos browser-flow URLs are public app routes.** `infra/kratos/kratos.yml` keeps portable defaults, while compose overrides `SELFSERVICE_*` values from `APP_BASE_URL` for production and from `capsulezero.local` for dev. Verification/recovery emails no longer carry `127.0.0.1` links in production compose.
- **2026-07-01 PR #57 review fix: OpenAPI auth schemes match the implemented token flow.** The canonical contract now advertises `Authorization: Bearer <session token>` and the alternate `X-Session-Token` header consumed by the Go API. Public auth endpoints opt out with `security: []`.
- **2026-07-01 PR #57 review fix: embedded auth migrations use pgx simple protocol.** The boot-time migrator still wraps each SQL file and its `schema_migrations` insert in one transaction, but the file body now runs with `pgx.QueryExecModeSimpleProtocol` so multi-statement migration files such as `0001_initial_auth.sql` are not rejected by pgx's default prepared-statement path.
- **2026-07-01 PR #57 review fix: the implemented auth/profile surface is now in the canonical API contract.** `api-spec.md`, `openapi.yaml`, and generated clients now include `/api/auth/registration`, `/api/auth/login`, `/api/auth/whoami`, `/api/auth/recovery`, and `/api/auth/logout`; the shared `Profile` schema matches the Go/web `api` provider shape, and Go error responses use the documented nested `ErrorResponse` envelope.
- **2026-07-01 PR #57 review fix: auth flow errors no longer collapse into false client success/failure.** Registration returns 400 only for Kratos self-service flow rejections and 502 for unexpected upstream/transport failures; logout now treats non-2xx Kratos responses as errors and surfaces a 502 instead of reporting success while a token may still be active.
- **2026-07-01 PR #57 review fix: session verification and web logout preserve outage signals without trapping local sign-out.** `/api/auth/whoami` still returns an empty 200 response for invalid/expired tokens, but Kratos transport/upstream failures now return 502. The web `api` provider still throws on failed `whoami` / logout responses; `signOutAction` treats logout revocation as best-effort and clears the local app cookie in a `finally` path so users can exit during an identity outage.
- **2026-07-01 PR #57 review fix: production Kratos SMTP is required.** Base compose now requires `KRATOS_SMTP_CONNECTION_URI` instead of falling back to dev-only MailHog; `docker-compose.dev.yml` owns the MailHog courier URI, and the env template documents the Resend SMTP shape.
- **2026-07-01 PR #57 review fix: API-mode sign-up carries the active locale.** The auth panel passes `useLocale()` into the sign-up action, the action forwards it through `PasswordCredentials`, and the API provider includes `locale` in `/api/auth/registration`, preserving RU sign-ups instead of defaulting all missing values to EN.
- **2026-07-01 PR #57 review fix: Kratos public self-service routes are reachable through nginx.** Both Docker nginx and the active host nginx route `/self-service/*` and `/sessions/*` to the Kratos public listener, so verification/recovery links generated from `KRATOS_PUBLIC_BASE_URL=https://capsulezero.app/` land on Kratos instead of the Next.js app. The Kratos admin API remains unproxied.
- **2026-07-01 PR #57 review fix: production Kratos no longer runs in dev mode.** `oryd/kratos:v1.1.0 serve --help` documents `--dev` as disabling critical security features, so base compose now runs `serve --config /etc/config/kratos/kratos.yml --watch-courier`; `docker-compose.dev.yml` keeps `--dev` for local-only development.
- **2026-07-01 PR #57 review fix: protected-route session middleware preserves Kratos outage signals.** `RequireSession` now returns 401 only for missing/invalid credentials and 502 for unexpected `WhoAmI` upstream failures, matching `/api/auth/whoami`.
- **2026-07-01 PR #57 review fix: web runtime env is explicitly allowlisted.** The `web` service no longer imports `./.env` as a service-level `env_file`; Compose still uses the droplet env for interpolation, but the container only receives the explicit keys under `services.web.environment`, keeping Postgres/Kratos/SMTP secrets out of the frontend runtime.
- **2026-07-01 PR #57 review fix: stale logout tokens are treated as already signed out.** Kratos logout client responses `400/401/403/404` now map to `ErrInvalidCredentials`; the auth handler returns `200 {"ok":true}` for that class so the web action can clear the local app session, while unexpected Kratos/logout outages still return 502.
- **2026-07-01 PR #57 review fix: identity session reads no longer refresh profile edit timestamps.** `EnsureForIdentity` uses a guarded upsert CTE: inserts and real Kratos-synced field changes update `updated_at`, but no-op session reads fall through to the existing row without mutating `updated_at`. Verified with Go tests and a live `postgres:16` smoke.
- **2026-07-01 PR #57 review fix: Kratos cipher secret guidance is exact-length.** Ory docs require `secrets.cipher` to be exactly 32 characters for `xchacha20-poly1305`, so the production env template now uses a 32-character sample value and documents `openssl rand -hex 16` instead of only `openssl rand -base64 32`.
- **2026-07-01 PR #57 review fix: recovery no longer sends one-time codes the UI cannot redeem.** The Phase 2 auth panel only starts email recovery and has no code/password completion step, so Kratos recovery now uses `link`, and the Go client submits `method: "link"` while keeping validation-style responses account-enumeration safe and unexpected courier/Kratos failures as 502. (**Superseded 2026-07-01** — `link` still lands the user on a UI that cannot complete the reset; the review fan-out escalated this to disabling recovery/verification entirely for the slice rather than shipping the dead-end. See the deferral decision below.)
- **2026-07-01 PR #57 review fix: retired Supabase keys are out of the production env contract.** `deploy/compose.env.example` now documents only the production-stack `api` provider path; rollback-only Supabase credentials stay with legacy rollback artifacts instead of the new droplet env file.
- **2026-07-01 PR #57 review fix: public auth writes are edge-rate-limited.** Both the active host nginx and rollback Docker nginx define `limit_req_zone $binary_remote_addr zone=capsule_auth_per_ip:10m rate=10r/m`, return 429 on throttling, and apply `limit_req ... burst=10 nodelay` to `/api/auth/registration`, `/api/auth/login`, and `/api/auth/recovery` before proxying to the Go API.
- **2026-07-01 PR #57 review fan-out (Codex P1): password recovery and email verification are deferred rather than shipped half-working.** The web auth UI has no flow-aware completion step — `app/src/app/[locale]/auth/page.tsx` never reads the Kratos `flow`/`code` params — so a recovery/verification email link would land the user on `/auth` (or, for link-recovery, `/profile`) with no way to finish the reset/verification. Instead of advertising a dead-end email flow, this slice **disables** the Kratos `recovery` and `verification` flows (`infra/kratos/kratos.yml`), drops the now-meaningless `SELFSERVICE_FLOWS_RECOVERY_UI_URL`/`VERIFICATION_UI_URL`/`VERIFICATION_AFTER_*` env from `docker-compose.yml` + `docker-compose.dev.yml`, and removes the "forgot password" affordance / recovery mode from `AuthPanel.tsx` (only sign-in/sign-up remain). Registration still auto-logs-in via the `session` hook. The recovery action/schema/`api`-provider method/Go `/api/auth/recovery` endpoint and the recovery i18n keys + CSS are intentionally **kept dormant** for the dedicated recovery/verification slice, which will add the completion UI plus the Go settings/verification endpoints. Alternative "build the completion UI now" was rejected as a separate slice-sized effort that would balloon this auth PR. Verified: `kratos.yml` YAML valid, `docker compose --env-file deploy/compose.env.example config` renders no recovery/verification UI URLs, `npm run typecheck` + `npm run lint` clean.
- **2026-07-01 PR #57 review fan-out (security HIGH): registration no longer discloses whether an account exists.** The Go Kratos client classifies a rejected registration by the stable Kratos UI message id `4000007` ("identifier exists") into `kratos.ErrIdentifierExists` (wrapping `ErrFlowRejected`, so login still collapses to a generic rejection). The auth handler returns the same generic `400 VALIDATION_ERROR` for that case as for any other rejected field instead of echoing Kratos's "an account already exists" text, closing the unauthenticated account-enumeration oracle (A07) that login and recovery already avoided. Genuine field-validation feedback (e.g. weak password) is preserved. Covered by `TestRegisterClassifiesRejections` (kratos) and `TestRegistrationDoesNotLeakAccountExistence` / `TestRegistrationPreservesValidationFeedback` (auth).
- **2026-07-01 PR #57 UX fix: auth server errors now read as errors.** Backend auth errors (e.g. wrong password) were rendered but in the muted secondary text colour, so they read as a neutral hint and were easy to miss. Root cause: `.auth-server-message` was a member of two rule groups in `globals.css` — the error-colour group and a later layout group that repainted it `--color-text-secondary`, and the later rule won (CSS cascade). Fix: dropped `.auth-server-message` from the error-colour group, added an `.auth-server-message-error` modifier (`--color-error` `#FFD600`, bold, per the design system), and `AuthPanel` now tracks message kind (`"error" | "info"`) — errors get the modifier plus `role="alert"` / `aria-live="assertive"`, info/success stay neutral and `polite`. Also dropped the trailing period from the "Invalid email or password" copy (Go API `auth.go` + the `api` provider fallback). Verified in the browser against the live api stack.
- **2026-07-01 PR #57 review fan-out (Codex P1): direct Kratos self-service auth paths can no longer bypass the Go API.** The broad `location ^~ /self-service/` (and `/sessions/`) nginx proxy forwarded every Kratos public path — including `/self-service/registration/api` and `/self-service/login/api` — straight to Kratos, letting an unauthenticated caller sidestep the Go handler's duplicate-identifier sanitization (A07) and the auth rate limit while receiving Kratos's raw responses. Since recovery/verification browser flows are deferred (disabled in `kratos.yml`), nothing browser-side needs the public Kratos surface this slice, and `app/src` makes zero direct Kratos calls. Fix: both `infra/nginx-host/capsulezero.app.conf` and `infra/nginx/conf.d/capsulezero.conf` now `return 404` for `^~ /self-service/` and `^~ /sessions/`, and the production `docker-compose.yml` no longer binds Kratos public (`4433`) to the host (the loopback bind existed only for the removed host-nginx proxy; the internal network still reaches `kratos:4433`). The dev override keeps `127.0.0.1:4433` for local inspection, and `KRATOS_PUBLIC_HOST_BIND` was dropped from `deploy/compose.env.example`. Alternative "deny only registration/login API paths" was rejected as narrower than needed given the deferral; the recovery/verification slice re-exposes only the exact public paths its completion UI needs. Verified: `nginx -t` passes both configs; `docker compose config` shows no host `ports:` on `kratos` in prod and the `127.0.0.1:4433` bind in dev.
- **2026-07-01 PR #57 review fan-out (Codex P2): auth throttling now also covers server-action-originated traffic, not just the edge path.** Because the web container calls `http://api:8080` over the private network, auth submissions from the Next.js server actions never traverse the host-nginx `/api/auth/(registration|login|recovery)` `limit_req`, so a bot posting to the web auth action route could drive Kratos login/registration/recovery unthrottled. Fix: a per-client token-bucket limiter (`api/internal/ratelimit`, 10/min, burst 10 — mirroring the edge) now wraps the registration/login/recovery handlers in `api/cmd/api/main.go`; on exhaustion it returns `429 RATE_LIMITED` with `Retry-After: 60`. The Go API is the single chokepoint every auth attempt funnels through, so throttling there covers both edge-direct and web-action-originated calls. To key per real client rather than per web container **without opening a spoofing hole**, the throttle key is derived only from a proxy source our own edge validates. The nginx `realip` module (`set_real_ip_from` Cloudflare ranges + `real_ip_header CF-Connecting-IP` + `real_ip_recursive on`, in `infra/nginx-host/00-capsule-zero.conf` and `infra/nginx/nginx.conf`) rewrites `$remote_addr`/`$binary_remote_addr` to the true client — but only for connections from a Cloudflare range, so a direct-to-origin `CF-Connecting-IP` forgery is ignored and keys on the attacker's real address. `app/src/lib/providers/api/index.ts` `apiFetch` then reads **only** `x-real-ip` (which host-nginx sets from the realip-corrected `$remote_addr` and overwrites every request — not `cf-connecting-ip`, which the web `location /` does not sanitize, and not the raw `x-forwarded-for`) and forwards it under the dedicated `X-Capsule-Client-IP` header; the Go limiter (`api/internal/ratelimit`) keys on that header only (RemoteAddr fallback), and both nginx configs overwrite any client-supplied `X-Capsule-Client-IP` with `$remote_addr` on `/api/*`. This closes the two Codex P1 follow-ups: (a) the earlier left-most-`X-Forwarded-For` key was rotatable per request, and (b) `$remote_addr`/`cf-connecting-ip` were untrustworthy behind Cloudflare (the POP shared one edge bucket across users, and a direct-origin caller could forge `CF-Connecting-IP`). Alternative "throttle the web action route at nginx" was rejected: Next.js server actions POST to the current page path with an `Next-Action` header, so there is no stable path to match. Covered by `internal/ratelimit` Go tests (burst→429, refill, per-key isolation, idle eviction, 429 envelope + `Retry-After`, distinct trusted clients, raw `X-Forwarded-For` ignored, trusted-header-preferred, RemoteAddr fallback); nginx realip + header handling by `nginx -t` on both configs; web derivation by `npm run typecheck` + `npm run lint`.
- **2026-07-01 PR #57 review fan-out (Codex P2): `PATCH /api/profile` is set-or-leave; `null` removed from the contract instead of advertising an unsupported clear.** `ProfileUpdateRequest` marked `country`/`city`/`avatarUrl` as `type: [string, "null"]`, but the Go handler decodes each into a `*string` and the `Apply` SQL uses `COALESCE($n, col)`, so a JSON `null` (nil pointer) is indistinguishable from an omitted field and leaves the stored value — a client clearing a saved city/avatar with `null` per the contract would get `200` with stale data. The actual web client already sends omit-only (`city: data.city || undefined` in `app/src/features/profile/actions.ts`), never `null`, so rather than wire a new clear-a-field feature end to end (web `null` intent + tri-state Go decode + `CASE WHEN` SQL) inside this auth slice, the contract was corrected to match the implementation and the client: `country`/`city`/`avatarUrl` are now `type: string` (non-nullable) in `docs_capsule_zero/adr/openapi.yaml`, and the web/mobile/app clients were regenerated. Verified: `npm run check:api-contract` (48 route-methods, clients drift-checked) + `npm run typecheck` + `npm run lint`.
- **2026-07-01 PR #57 review fan-out (Codex P2): `PATCH /api/profile` validates fields against the contract before persisting.** The handler forwarded decoded strings straight to `Profiles.Apply`, which only normalizes `locale`, so a non-web/direct API client sending `displayName: ""` (violating `minLength: 1`) or a `country`/`city` longer than the OpenAPI `maxLength: 80` would get a `200` with invalid data stored. Fix: `api/internal/auth/auth.go` now runs `validateProfileUpdate` in `PatchProfile` before `Apply` — a provided `displayName` must be non-blank and ≤ 80 runes, and a provided `country`/`city` must be ≤ 80 runes; a violation returns `400 VALIDATION_ERROR` and never touches the repository (`locale` stays validated in `Apply`). Only provided (non-nil) fields are checked, preserving the set-or-leave semantics. Covered by `TestPatchProfileRejectsInvalidFields` (empty/whitespace/over-long display name, over-long country/city → 400 before `Apply`, proven by a nil `Profiles`) and `TestValidateProfileUpdateAcceptsValidAndOmitted`.
- **2026-07-01 PR #57 review fan-out (Codex P2 ×2): profile display-name text is trimmed and validated on every write path.** `Registration` passed `name` straight to Kratos (identity schema permits 256 chars), and `EnsureForIdentity` persists that trait as `display_name` — so a sign-up `name` > 80 chars, or a whitespace-only `name` like `"   "`, would back-door an over-long or blank profile display name past the PATCH validation. Fix: `Registration` now trims `name`, rejects a whitespace-only value (`400`), and bounds it to ≤ 80 runes before `Kratos.Register` (name stays optional — empty/omitted still accepted). For symmetry the same normalization was applied to `PatchProfile`: `displayName`/`country`/`city` are trimmed (via `trimOptional`) before `validateProfileUpdate` and before `Apply`, so surrounding whitespace is neither stored verbatim nor able to slip a blank display name past the check. Covered by `TestRegistrationRejectsOverLongName`, `TestRegistrationRejectsWhitespaceName`, and the `whitespace display name` case in `TestPatchProfileRejectsInvalidFields`.
- **2026-07-01 PR #57 review fan-out (Codex P2): auth sessions return the edited display name, not a stale Kratos trait.** `buildUser` preferred `session.Identity.Traits.Name.First` over `profile.DisplayName`, so after a user changed `displayName` via `PATCH /api/profile` (which `EnsureForIdentity` intentionally preserves), `whoami`/sign-in — and `readVerifiedAppSession` on the web — surfaced the original registration name. Fix: `buildUser` now prefers `profile.DisplayName` and falls back to the Kratos trait only when no display name is set yet. Covered by `TestBuildUserPrefersEditedDisplayName`.
- **2026-07-01 PR #57 review fan-out (Codex P2): the Go API module is covered by the required `test` check.** The required workflows (`ci.yml`, `test.yml`, `pr-guard.yml`) only installed/built the Node app + Playwright e2e; nothing ran `go test`/`go vet` under the nested `api/` module, so auth/profile/rate-limit/migration changes could merge untested. Fix: `.github/workflows/test.yml` now runs `go vet ./...` + `go test ./...` in `api/` via the pinned `golang:1.25.11-bookworm` image (same toolchain as local verification, no unpinned `setup-go` action) as a step of the required `test` job. This is infra/delivery wiring (not TDD-gated), verified by config validation + the run itself.
- **2026-07-01 PR #57 review fan-out (Codex P2): the documented local-dev compose command is runnable.** `docker compose -f docker-compose.yml -f docker-compose.dev.yml up` aborted because the base file's production `${VAR:?...}` interpolations (`POSTGRES_USER`, `KRATOS_DSN`, `API_DATABASE_URL`, `SESSION_SIGNING_SECRET`, …) are evaluated when the base file loads — an override file's `:-` default cannot rescue them (confirmed: `docker compose -f base -f dev config` fails on the first required var). Rather than weaken the base's fail-fast prod safety, added `deploy/compose.dev.env` with dev-only throwaway credentials and updated the documented command to `docker compose --env-file deploy/compose.dev.env -f docker-compose.yml -f docker-compose.dev.yml up …` (dev override still layers MailHog/`--dev`/`capsulezero.local`/loopback binds on top). Docs actualized (`docker-compose.dev.yml` header, `docker-compose-deploy.md`, `nginx-reverse-proxy.md`, `CLAUDE.md`). Verified: `docker compose --env-file deploy/compose.dev.env -f docker-compose.yml -f docker-compose.dev.yml config` renders; prod `docker compose --env-file deploy/compose.env.example config` still renders.
- **2026-07-02 merge with `main` (#60/#61/#62/#63) resolved doc-by-doc, not side-by-side.** Spec-024 memory keeps the PR side (Phase 2 is the shipped auth vertical slice, rows 9a–9q); infra/runtime docs keep main's actualizations (host-nginx edge, profile-gated `docker-edge` rollback, `app/` paths, ADR-007 deferrals) with the PR's embedded-SQL-migrator wording re-applied on top; the stale generated clients under `web/src/` and `mobile/lib/` that main deleted stay deleted. `deploy/compose.dev.env` is now actually committed via a `.gitignore` exception — the earlier fix added the file locally but `deploy/*.env` swallowed it, which is exactly the Codex P2 on `docker-compose.dev.yml`.
- **2026-07-02 PR #57 review fan-out (architect P2): raced first sign-ins no longer surface a false 500.** Two concurrent `EnsureForIdentity` calls for a brand-new identity could both miss: the loser's `ON CONFLICT` waits for the winner's commit, no-ops under the update guard (the upsert CTE returns no row), and the fallback `SELECT` runs on the statement snapshot taken before that commit — `pgx.ErrNoRows` → 500. Fix: one retry on `ErrNotFound` re-executes on a fresh snapshot. Alternative "make `DO UPDATE` unconditional with `SET updated_at = profiles.updated_at`" was rejected: `EnsureForIdentity` runs on every session resolution, so an unconditional update would write a dead tuple per request (MVCC churn) to cover a rare race. Covered by `TestEnsureForIdentityRetriesRacedFirstSignIn` / `TestEnsureForIdentityDoesNotRetryOtherErrors`.
- **2026-07-02 PR #57 review fan-out (security MEDIUM/LOW): `PATCH /api/profile` validates `avatarUrl` and rejects blank `country`/`city`.** `avatarUrl` previously accepted any string — a `javascript:`/`data:`/internal-address value would be stored and returned to every consumer (stored-XSS/SSRF vector for future `<img>` renderers or server-side fetchers); a whitespace-only `country`/`city` silently stored an empty string (an accidental clear that set-or-leave explicitly does not support). Now `avatarUrl` must be an absolute `https` URL ≤ 2048 runes, provided-but-blank `country`/`city` return `400 VALIDATION_ERROR`, and `ProfileUpdateRequest` carries matching `minLength`/`format: uri`/`maxLength` in `openapi.yaml` with the client regenerated. Covered by the new `TestPatchProfileRejectsInvalidFields` cases.
- **2026-07-02 PR #57 review fan-out (misc LOW + docs):** `apiFetch` now tolerates a non-JSON upstream body (nginx 502 HTML while the API restarts) and resolves to status-based error handling instead of throwing `SyntaxError` (closes fan-out follow-up 4); `signOutAction` logs a failed best-effort revocation instead of returning `ok: true` with an error-shaped `message`; `authActionErrorMessage` strips only a leading `CODE:` prefix instead of splitting on the last `:` (colons inside human-readable text survive); `adr-002-auth.md` no longer claims recovery/verification are active in v0.1 (critic finding — the ADR contradicted `infra/kratos/kratos.yml`); the Kratos `settings` flow is documented as intentionally inert (see Known Issues — the config schema rejects `settings.enabled`).
- **2026-07-02 PR #57 Codex P2: the profile form limit is aligned with the API display-name limit.** `createProfileFormSchema` validated `firstName`/`lastName` independently (≤ 40 each), so a form-valid 40+40 pair joined with a space produced an 81-rune `displayName` that the new 80-rune API guard rejects with `400` — and `saveProfileAction` called `updateProfile` without catching provider errors, so the rejection surfaced as an unhandled server-action error instead of the form's save-error path. Fix: a `superRefine` on the schema rejects a combined name over 80 runes inline (`validation.displayNameLength`, EN + RU), and `saveProfileAction` wraps `updateProfile` in try/catch returning `PROFILE_SAVE_FAILED` so any provider rejection lands in the existing `notice.saveError` path.
- **2026-07-02 PR #57 Codex P2 ×3 (fcc7d3f review):** (1) **a provided-but-blank `locale` in `PATCH /api/profile` is rejected** — it previously fell through to `NormalizeLocale`, whose `""→"en"` default exists for omitted registration/identity traits, so `{"locale":""}` silently reset a RU profile to EN with a value outside the OpenAPI `Locale` enum; the handler now trims `locale` via `trimOptional` and `validateProfileUpdate` returns `400` for blank (enum validation stays in `Apply`). Covered by the `blank locale`/`whitespace locale` cases in `TestPatchProfileRejectsInvalidFields`. (2) **`/api/auth/recovery` is no longer published as a live endpoint** — the contract documented a 200 acceptance while this same slice disables Kratos recovery, so any client following the contract would hit a 502; the operation is now `deprecated: true` with a description stating it is dormant (fails 502) until the recovery/verification slice, and `api-spec.md` prose + route table say the same. Removing the path entirely was rejected: the Go endpoint and provider plumbing are deliberately kept dormant (see the deferral decision above), and the contract entry documents that surface. (3) **plan.md verification row 20 no longer contradicts the no-rename decision** — it still said Phase 6 removes `/app` and builds from `/web/Dockerfile` (a stale PR-side row that survived the merge); the criterion/evidence now match the accepted Phase 6 scope (`/app` stays, the `/web` placeholder and `docker-compose.legacy-supabase.yml` are deleted, Supabase provider fully removed).
- **2026-07-02 PR #57 Codex P2 (c0ef6f1 review): explicit JSON `null` in `PATCH /api/profile` returns `400` instead of a silent no-op.** `encoding/json` decodes `{"country":null}` into the same nil `*string` as an omitted field, so a null-bearing request skipped `validateProfileUpdate`, `Apply` treated it as "leave unchanged", and the API answered `200` while `ProfileUpdateRequest` says null is not accepted. Fix: `httpx.DecodeJSONObject` decodes the body once and also returns the raw field values; `PatchProfile` rejects any of the five contract fields whose raw value is JSON `null` (`httpx.IsJSONNull`) before validation. Alternative "tri-state decode + `CASE WHEN` SQL to make null actually clear the field" stays rejected — clearing is a deferred product feature (see the set-or-leave decision above); the contract now fails loudly instead of lying. Covered by the `null display name`/`null locale`/`null country`/`null city`/`null avatar` cases in `TestPatchProfileRejectsInvalidFields`.
- **2026-07-10 spec 039: DigitalOcean Spaces is superseded by Hetzner Object Storage.** The production stack had already moved from DigitalOcean compute to Hetzner (spec 033), and storage now follows the same provider boundary: private uploads, public catalog assets, and encrypted backups use Hetzner's S3-compatible Object Storage with provider-neutral `OBJECT_STORAGE_*` / `BACKUP_S3_*` env keys. Local root disk and a single-node MinIO were rejected for durable user assets; Hetzner Volumes remain possible working storage for future model/runtime data, not the object-store boundary. Phase 4/5 acceptance rows were rewritten to avoid promising Spaces CDN URLs or a `s3://capsulezero/backups/` bucket shape.
- **2026-07-10 spec 040: the storage runtime is split from later UI/item attachment.** The private Go adapter, storage-gated health, and unattached original-photo init/complete foundation land here. Asset buckets are provisioned in project `15203114` / HEL and the Object-Locked backup bucket in isolated project `15296835` / FSN. Exact production CORS/negative probes, absent backup CORS, and the first same-project-key signed smoke passed. Frontend upload UI, public catalog delivery, encrypted backup automation, and imgproxy are not claimed complete.
- **2026-07-11 spec 040 key hardening:** runtime credentials moved to bucketless project `15302873` and backup credentials to bucketless project `15302925`; both return `list-buckets=0`. Cross-project policy/readback and live probes passed runtime `ListBucket` plus prefix-bounded Put/Get/Delete, public runtime-principal `s3:*` denial, and runtime control-plane/cross-bucket denial. The backup policy passed a normal prefix-bounded Put and explicit denials for object/version reads, ACL get/put, retention/legal-hold get/put, object/version deletes, governance bypass, bucket/version/multipart listing, policy/CORS/lock-config reads, outside-prefix/assets writes, dangerous canned ACLs, and AllUsers grant-read. Hetzner's Put-only cross-project grant unexpectedly allowed Head/Get, and Hetzner/RGW still accepts Object Lock mode/retain-until/legal-hold headers on the original `PutObject` despite direct action denies. That cannot expose/delete existing data but remains a bounded write-time storage-DoS/cost gate for Phase-5 backup automation. The protected env was rotated atomically and remains `root:root` mode `600` with uploads false. The superseded same-project runtime/backup keys and both temporary policy operators were deleted; the post-revocation rotated-key 10 MiB PUT/HEAD/GET/checksum/delete smoke passed with cleanup. Exact-origin private/public CORS, attacker-origin denial, and absent backup CORS were reconfirmed. Quota/orphan cleanup/wardrobe attachment continue to gate upload activation.

### Known Issues

- **(Phase 5) The isolated backup writer cannot read/delete/list existing data or directly mutate ACL/Object Lock state, but Hetzner/RGW authorizes Object Lock headers carried by an allowed `PutObject`.** A compromised writer could create retained/held versions and amplify storage cost. Resolved for activation by spec 047: the root-owned uploader constructs a fixed local header set with no caller-controlled metadata, the residual is explicitly accepted, every stream is `age`-encrypted, and retention + a full restore drill passed before the daily timer was enabled.

- The retired Supabase provider stays in `/app` only for domains not yet owned by the Go API. `/app` remains canonical; removal is domain-by-domain provider retirement, not a frontend rename.
- React Native scaffold ships in this spec, but real auth integration with Kratos lands in a follow-up auth/profile slice.
- Sentry and Prometheus are deferred to Stage 2. v0.1 observability is syslog + OTLP traces only; Grafana is also deferred by ADR-007 until its promotion trigger fires.
- Lava.top remains stubbed in `/api/billing/*` — the routes exist with stub responses so the OpenAPI contract is stable, but no real money moves until v0.2.
- Self-hosted Capsule Zero image-processing model is deferred to Stage 2 — v0.1 stores originals only and the 5 second processing gate is dormant.
- `npm run check:runtime-env` may still validate legacy Supabase/Lava/Photoroom env keys until the matching provider slices are retired. It is **not** invoked from CI (`ci.yml` runs only `check:repo`, `check:api-contract`, `lint`, `typecheck`, `build`, `docker build`, `npm test`). Phase 6 retires the Supabase env surface domain by domain.
- HTTP/3 / QUIC is unavailable at the origin while nginx-alpine ships without the QUIC build. Cloudflare gives HTTP/3 at the edge once the proxy is on. No action expected until then.
- During the Phase 1 droplet rollout there is a brief connection-refused window between `systemctl stop caddy` and `docker compose up -d` while certbot issues the cert. This is acceptable today because the droplet has no production traffic.
- **(Phase 2) `postgres` is added to the root `docker-compose.yml` only, not to `docker-compose.dev-server.yml`.** The remote `dev.capsulezero.app` stack stays web-only until the slice rolls out there; bringing Postgres up on the droplet is an operator rollout step, exactly like the Phase 1 first-rollout row. Local `docker compose up -d postgres` is the evidence.
- **(Phase 2) The app database uses the Postgres superuser role.** Least-privilege separation (a non-superuser `app` role owning the app schema) is a deferred hardening follow-up; tracked here so it is not silently forgotten. **Resolved by spec 034** — `capsule_app` owns the app database (fresh-init provisioning in `00-kratos-db.sh` + one-time prod rollout in `prod-cd-pipeline.md`).
- **(Phase 2) nginx `/api/*` shadows the Next.js `/api/health` route for browser traffic.** Benign: nothing in the browser calls `/api/*` (the `api` provider talks to the Go API server-side), so only the app's status route is shadowed, and it is reachable directly on the web container. If a future client-side call needs a Next route under `/api`, namespace the Go API (e.g. `/api/v1`) or the Next route.
- **(Phase 2) In `api` mode, unmigrated domains (wardrobe/capsule/catalog/billing) serve mock fixtures even in production builds.** Intentional for the incremental migration — the app is navigable with real auth while those domains move slice by slice. Not a launch state; tracked so it is retired before commercial launch.
- **(Phase 2) Password recovery and email verification are deferred, not deleted.** The Kratos recovery/verification flows are disabled and the auth UI shows no recovery affordance, because there is no flow-aware completion UI or Go settings/verification endpoint yet. The recovery action (`requestPasswordRecoveryAction`), schema (`createRecoverySchema`), `api`-provider `requestPasswordRecovery`, the Go `/api/auth/recovery` endpoint, and the recovery i18n keys (`forgotPassword`, `recovery*`) + `.auth-forgot-row`/`.auth-recovery-*` CSS are intentionally retained dormant so the follow-up slice re-wires them instead of rebuilding. Tracked so the dormant surface is not mistaken for live behavior.
- **(Phase 2) nginx `auth_request` is wired for routing but not yet enforcing.** There is no server-rendered protected route to guard yet (the dashboard resolves the session in-app via the Go API). The `auth_request` guard lands with the first route that needs edge-level protection.
- **(Phase 2) Registration still carries a residual account-enumeration signal via response shape, not message text.** The explicit "account exists" text disclosure is closed, but because v0.1 auto-logs-in on sign-up (`session` hook, no required verification), a fresh email returns `200` + session while a taken email returns `400 VALIDATION_ERROR`. An attacker submitting a known-policy-valid password can still distinguish existence by status code. Fully closing this requires verification-gated sign-up (drop instant login) or Kratos-level enumeration protection — a product decision deferred out of this slice. Accepted residual for v0.1.
- **(Phase 2) Clearing a saved profile field is not supported — `PATCH /api/profile` is set-or-leave.** `country`/`city`/`avatarUrl` can be set or left unchanged (omit), but there is no way to blank a previously saved value: the web sends omit-only and the Go handler treats a missing pointer as "leave" via `COALESCE`. `null` was removed from `ProfileUpdateRequest` so the contract no longer advertises a clear it cannot perform, and an explicit JSON `null` now returns `400 VALIDATION_ERROR` instead of silently reading as an omitted field. Wiring an explicit clear (web `null` intent → tri-state Go decode → `CASE WHEN` SQL) is a deferred follow-up if the product needs it.
- **(Phase 2) The Go auth rate limiter is in-memory, and its trusted client key depends on the embedded Cloudflare IP ranges staying current.** Two accepted residuals: (a) the bucket store is per-process, so it does not throttle across replicas — fine on the single-server v0.1 topology, revisit with a shared Redis store on scale-out; (b) the active `set_real_ip_from` Cloudflare ranges in `infra/nginx-host/00-capsule-zero.conf` + `infra/nginx/nginx.conf` are a static snapshot of `https://www.cloudflare.com/ips/`. If Cloudflare adds a range not listed, requests from it would fall back to keying on the POP address (over-throttle, fail-safe — not a bypass); if a listed range were ever reassigned away from Cloudflare, a caller there could spoof `CF-Connecting-IP`. Spec 047 refreshed the snapshot at activation; refresh it and both origin firewalls whenever Cloudflare publishes a change. Direct-to-origin spoofing from a non-Cloudflare address is closed by the origin firewall and realip trust boundary.
- **(Phase 2) Follow-ups from the PR #57 review fan-out (non-blocking):** (1) `/api/auth/whoami`, `/api/auth/logout`, and `/api/profile` are still outside the throttled set (the Go limiter covers registration/login/recovery; whoami/logout/profile fall into the unthrottled generic `/api/` nginx location) — extend the limiter or add an `^~ /api/auth/` edge zone to session-token validation paths; (2) Kratos Argon2 cost is `iterations: 1` (below OWASP `t≥2`) — raise before real-provider QA; (3) the generated OpenAPI client (`app/src/lib/api/generated/` — the only one left after main's `/web` + `mobile/lib` cleanup) is drift-checked against `openapi.yaml` but not against the Go handlers — wire an `openapi.yaml ↔ Go` contract guard into `check-api-contract.mjs` or defer generation until a real consumer; (4) `profiles.email` is indexed but not `UNIQUE` and the boot migrator takes no `pg_advisory_lock` (safe on single-replica, revisit on scale-out). **All four resolved by spec 034** (session-bucket limiter in the Go API, `iterations: 2` with a dev-only override, Go-route guard in `check-api-contract.mjs`, `profiles_email_key` UNIQUE + advisory-locked migrator).
- **(Phase 2) The Kratos `settings` self-service flow stays configured but intentionally inert.** Kratos v1.1.0 cannot disable it: the config schema rejects `selfservice.flows.settings.enabled` (verified against `oryd/kratos:v1.1.0` — boot fails with `additionalProperties "enabled" not allowed`), unlike recovery/verification. It is unreachable in production regardless: the edge returns 404 for `/self-service/*` and neither the web app nor the Go API drives a settings flow. The dedicated profile/settings slice ships its completion UI before the edge re-exposes it.
