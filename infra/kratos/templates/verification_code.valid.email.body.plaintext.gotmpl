Hi,

please confirm your Capsule Zero email address.

Your confirmation code: {{ .VerificationCode }}

Or open this link to confirm instantly:
{{- $locale := "en" }}
{{- if .Identity }}{{ with index .Identity.traits "locale" }}{{ $locale = . }}{{ end }}{{ end }}
{{ .VerificationURL | replace "/self-service/verification?" (printf "/%s/verify-email?" $locale) }}

The code and link expire in 1 hour. If you did not create a Capsule Zero
account, you can safely ignore this email.
