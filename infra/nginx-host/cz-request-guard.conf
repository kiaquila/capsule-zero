# Safe server-context return checks. Keep ACME reachable: its path does not
# match the probe classifier and uses GET/HEAD.
if ($cz_method_allowed = 0) { return 405; }
if ($cz_block_probe = 1) { return 404; }

limit_conn cz_conn_per_ip 30;
limit_req zone=cz_general_per_ip burst=60 nodelay;
